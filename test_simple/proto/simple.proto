syntax = "proto3";
package proto;
option go_package = ".;proto";

service SimpleServer {
  rpc Simple(Request) returns (Response);
}

message Request {
  string msg = 1;
}

message Response {
  string msg = 1;
}
